# Project Management

## Run Locally

Go to the server directory

```bash
cd server
```

Copy .env.example to .env

```bash
cp .env.example .env
```

Back to root directory

```bash
cd ..
```

Run docker compose

```bash
docker compose up -d --build
```
